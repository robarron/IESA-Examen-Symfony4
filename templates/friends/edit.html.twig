{% extends 'base.html.twig' %}

{% block title %}Edit Friends{% endblock %}

{% block body %}
    <h1>Edit Friends</h1>

    {{ include('friends/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('friends_index') }}">back to list</a>

    {{ include('friends/_delete_form.html.twig') }}
{% endblock %}